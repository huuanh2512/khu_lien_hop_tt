# Copy this file to .env and replace the placeholder values before running locally or deploying.

MONGODB_URI=mongodb+srv://user:password@cluster0.mongodb.net/khu_lien_hop_tt
MONGODB_DB_NAME=khu_lien_hop_tt
JWT_SECRET=your_jwt_secret_here

FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nABC...\n-----END PRIVATE KEY-----

CORS_ORIGINS=http://localhost:3000,http://localhost:5173

AUTO_CANCEL_PENDING_MINUTES=10

# Notes:
# - FIREBASE_PRIVATE_KEY must stay on a single line (use literal \n) when configuring platforms like Render.
# - Remember to copy this template to .env and fill in real credentials before starting the server.
